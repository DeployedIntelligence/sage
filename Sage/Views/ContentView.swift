import SwiftUI

/// Root view — will be replaced with tab bar / onboarding flow in later sprints.
struct ContentView: View {
    var body: some View {
        Text("Sage")
            .font(.largeTitle)
            .fontWeight(.bold)
    }
}

#Preview {
    ContentView()
}
